# Matrix Factorization model configuration

# Model parameters
model_type: mf
trainer_type: sampling
scenario: warm

# Hyperparameters
embed_dim: 8
batch_size: 1024
learning_rate: 0.005
weight_decay: 0.1
epochs: 10
biases: user,item,category,store